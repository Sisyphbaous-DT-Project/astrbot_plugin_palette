# v0.4.4

## 新增

- 新增横屏/竖屏壁纸分池，支持分别上传、选择和删除横屏壁纸与竖屏壁纸。
- Dashboard 会根据当前视口方向选择对应壁纸，横竖屏切换时会预加载目标壁纸并叠化过渡。
- “打开或刷新时随机背景”会优先随机当前方向图库，旋转屏幕不会再次随机。

## 兼容性

- 保留旧版 `background_image` / `background_images` 配置作为兼容兜底。
- 某个方向图库为空时，会自动回退到旧背景或另一方向壁纸，避免出现空白背景。
- 旧版单图库默认归入横屏图库；删除图片会同步移除横屏、竖屏和旧图库里的同名引用，避免残留失效配置。
