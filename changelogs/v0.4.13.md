# v0.4.13

## 新增

- 在插件根目录加入 `logo.png`，保持 1:1 比例并采用推荐的 `256x256` PNG 尺寸，AstrBot 插件市场和插件列表会自动读取该图标。
- README 标题区域新增插件图标展示。

## 文档

- 同步 `metadata.yaml`、运行时版本常量、README 和 CHANGELOG 的版本号至 `0.4.13`。

## 兼容性

- 不新增配置项或插件 API，最低 AstrBot 版本仍为 `>=4.26.0-beta1`。
